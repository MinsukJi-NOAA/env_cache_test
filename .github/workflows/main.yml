name: Test

on:
  push:
    branches: ['master']
  pull_request:
    branches: ['master']

jobs:
  job1:
    name: job 1
    runs-on: ubuntu-20.04

    steps:
    - name: checkout codes
      uses: actions/checkout@v2

    - name: set env
      run: |
        echo "LETS_SEE=$((1+1))" >> $GITHUB_ENV
        echo "1: $LETS_SEE"

    - name: check env in another step
      run: echo "2: $LETS_SEE"

  job2:
    name: job 2
    runs-on: ubuntu-20.04

    steps:
    -name: check env in another job
     run: echo "4: $LETS_SEE"
  
#        echo "LETS_SEE=yes" >> $GITHUB_ENV
#    - name: test environment variable
#      run: |
#        echo "== HELLO from GHA =="
#        echo $LETS_SEE
#        ./ms.sh
#        echo $LETS_SEE
#        #pip3 install PyGithub==1.54.1
#
#    - name: subsequent step
#      run: echo $LETS_SEE
